{
  "name": "slopsmith-plugin-subway-scaler",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "dev": "docker compose -f docker-compose.dev.yml up",
    "dev:down": "docker compose -f docker-compose.dev.yml down"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "18": "0.0.0"
  }
}
